#include <stdio.h>

char	*ft_strcat(char *dest, char *src);

int main ()
{
	char str[80];
	ft_strcat(str,"these ");
	/*ft_strcat(str,"strings ");
	ft_strcat(str,"are ");
	ft_strcat(str,"concatenated.");
	printf("%s", str);*/
}
